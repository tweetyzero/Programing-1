#include <stdio.h>
#define año = 2017
#define pi = 3'14
int main()                           //deberia haber un int delante
{
float radio, _radio, circunferencia;       // la variable float no puede ir con tilde
_radio = 2.5;                     // la variable Radio no esta definida Radio
radio = 2 * _radio;        // la variable no esta definida 2radio
PI = PI * radio;              //la variable PI no esta definida deberia ser pi
circunferencia = 2 * PI * _radio;                   //la variable Área no esta definida y no se puede poner tilde, PI tampoco esta definida
printf("el radio del circulo es %f\n", radio); //la variable Radio no esta definida
printf("el area del circulo es %f\n", area);   //la variable Área no esta definida y deberia der %f
printf("programa realizado en %d\n", año);
}
